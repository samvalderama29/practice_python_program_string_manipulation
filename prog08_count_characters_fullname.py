# Prog08: Create a program that ask the user to input their fullname.
#         Print the number of characters in the input.

# Display message to indicate the purpose of the program
print("No. of characters in a full name")

# Prompt the user to enter their full name
fullname = input("Enter your full name: ")

# Use len() to count the total number of characters (including spaces) in the full name and print the result
print(len(fullname))